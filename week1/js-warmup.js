


const ingredients = ['butter', 'milk', 'egg']
const INGREDIENTS = ingredients.map(ingredient => ingredient.toUpperCase())
const lingredients = ingredients.map(ingredient => `<li>${ingredient}</li>`)

